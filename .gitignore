# Android and Gradle
local.properties
.gradle/
build/
captures/
.externalNativeBuild/
.cxx/

# IntelliJ and Android Studio
.idea/
*.iml

# Keystores and secrets
*.jks
*.keystore
keystore.properties

# Content working files (raw downloads, extraction, built database)
content/raw/
content/audio/
content/packs/
content/work/
content/build/

# OS noise
.DS_Store
Thumbs.db

# The content database is a derived artifact: it lives in the project's
# Releases, addressed by its own hash, and is fetched by tools fetch.
content/quran.db

# Pack files are content-addressed Release assets, not repository files.
content/packs/

# The hand-off bundle: a large binary that lives here only until Play has it.
play-store/*.aab
