<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The player lives with the feature that owns it. The permissions it
         needs stay in the app's manifest, where the app's policy surface is
         read and audited. -->
    <application>
        <service
            android:name="io.github.muntasimulhaque.quran.playback.PlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>
    </application>
</manifest>
